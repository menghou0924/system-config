# Installation guide for meteor

# 1. install the application
curl https://install.meteor.com/ | sh

# 2. install node.js and nvm
brew install node

# 3. for meteor 0.9 before, install meteorite
npm install -g meteorite

# 4. install the packages at the meteor folder
mrt install

